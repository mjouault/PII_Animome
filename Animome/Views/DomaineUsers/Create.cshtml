@model Animome.ViewModels.DomaineUserCreateViewModel

@{
    ViewData["Title"] = "CreerDomaine";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Domaine</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="POST">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <p>
                    <select asp-for="Domaine.Intitule" asp-items="Model.Domaines">
                        <option value="">All</option>
                    </select>
                </p>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" asp-route-id="@ViewData["idUser"]" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 