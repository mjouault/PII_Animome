@model IEnumerable<Animome.Models.Commentaire>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>
    <a asp-controller="Suivis" asp-action="AfficherSuivi" asp-route-id="@ViewData["idPatient"]"><ion-icon name="arrow-back" stye=""></ion-icon></a>
</p>
<h1>Index</h1>
<p>
    <a asp-action="Create" asp-route-id="@ViewData["idSuivi"]"> Ajouter <ion-icon name="add-circle" stye=""></ion-icon></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Texte)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Texte)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id"><ion-icon name="create-outline"></ion-icon></a> |
                    <a asp-action="Delete" asp-route-id="@item.Id"><ion-icon name="trash-outline"></ion-icon></a>
                </td>
            </tr>
        }
    </tbody>
</table>
